---
description: Show the latest llm-router wrapper and tmux-team validation status.
allowed-tools: [Bash, Read]
---

# /router-status

Run:

```bash
bash "$HOME/.claude/plugins/llm-router/tools/router_status.sh"
```

Then summarize the result briefly with:
- overall router readiness
- latest wrapper pass/fail
- latest team/tmux pass/fail
- direct paths to the latest reports
